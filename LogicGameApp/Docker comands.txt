# Создать Docker image.
docker build . -t logic-server

# Запустить контейнер
docker run -p 8088:8080 --name logic logic-server -rm
docker run -p 8088:8080 --name logic logic-server -d
curl http://172.17.0.2:8080/Game/NewGame
